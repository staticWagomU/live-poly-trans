node_modules/
.env
build/
.svelte-kit/

src-tauri/target/
# v1 build artifacts. These keep their old names on purpose: they are what
# the v1 branch actually produces, and they are still sitting in the worktree.
src-tauri/binaries/helper-*
src-tauri/binaries/lpt-whisper-engine-*
src-tauri/binaries/liblpt_whisper_backend.*
src-tauri/binaries/LivePolyTransHelper.app/
src-tauri/icons/dmg-background.png
src-tauri/gen/

swift-helper/.build/

references/
.DS_Store

.direnv

# 外部ツール(cclens)のローカルDB
cclens.db

# v2 spike artifacts
/target
/models
/assets
